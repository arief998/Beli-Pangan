<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.belipangan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AuthenticationApp"
        tools:replace="android:allowBackup">
        <activity android:name=".BuyerProgressDetailActivity"/>
        <activity android:name=".BuyerPendingDetailActivity"/>
        <activity android:name=".BuyerFinishOrderActivity" />
        <activity android:name=".BuyerProgressOrderActivity" />
        <activity android:name=".BuyerPendingOrderActivity" />
        <activity android:name=".ProgressOrderDetailActivity" />
        <activity android:name=".PendingOrderDetailActivity" />
        <activity android:name=".SellerFinishOrderActivity" />
        <activity android:name=".SellerProgressOrderActivity" />
        <activity android:name=".SellerPendingOrderActivity" />
        <activity android:name=".ProductOrderActivity" />
        <activity android:name=".EditProductActivity" />
        <activity android:name=".MainActivityBuyer" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterBuyerActivity" />
        <activity android:name=".RegisterSellerActivity" />
        <activity android:name=".MainActivitySeller" />
        <activity android:name=".ProductDetailBuyerActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>