<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RegisterActivity">

    <Button
        android:id="@+id/btnRegisSeller"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Daftar Sebagai Penjual"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="250dp"
        android:onClick="registerSeller"/>

    <Button
        android:id="@+id/btnRegisBuyer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Daftar Sebagai Pembeli"
        app:layout_constraintTop_toBottomOf="@id/btnRegisSeller"
        app:layout_constraintStart_toStartOf="@id/btnRegisSeller"
        app:layout_constraintEnd_toEndOf="@id/btnRegisSeller"
        android:layout_marginTop="16dp"
        android:onClick="registerBuyer"/>

</androidx.constraintlayout.widget.ConstraintLayout>